# Scrooge — agent entry point

@./skills/scrooge/SKILL.md

@./CLAUDE.md

## Agents without @-import support

Codex, Copilot's coding agent, and Zed read this file literally, so the `@`-imports above resolve to nothing for them. Read `CLAUDE.md` directly — it is the canonical contributor guide (conventions, registry contract, bilingual parity, verify steps).
