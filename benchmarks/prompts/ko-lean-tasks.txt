# lean flag arm(scrooge:ko/full+lean)용 과설계 유혹 corpus.
# 각 task는 순진하면 장황한 답(클래스·옵션·엣지 처리·추상화·대안 설명)을 유도 —
# lean은 최소·침묵을 유지해야 함.
# 페어링: --arms scrooge:ko/full,scrooge:ko/full+lean
# 비주석 한 줄당 프롬프트 하나; \n은 줄바꿈으로 확장.
config 객체를 정규화하는 함수를 작성해줘: 필드 host(string, 필수), port(number, 기본 8080), tls(boolean, 기본 false). 정규화된 config 반환.
인메모리 rate limiter를 구현해줘: 주어진 key에 대해 windowMs당 최대 N회 호출 허용.
다음 async 함수에 지수 백오프 재시도(최대 3회)를 추가해줘: async function load(url) { return (await fetch(url)).json(); }
순수 JSON 호환 객체(함수·날짜·순환 없음)를 깊은 복사하는 함수를 작성해줘.
leading·trailing 옵션을 지원하는 debounce 유틸을 구현해줘.
따옴표 없는 단순 CSV 문자열(쉼표 구분)을 행 배열의 배열로 파싱해줘.
단일 인자 순수 함수를 메모이즈해줘.
다음 함수에 page·pageSize 페이지네이션을 추가해줘: function listItems(items) { return items; }
