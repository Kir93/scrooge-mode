# Scrooge benchmark — HI report corpus (HELD-OUT)
# One prompt per non-comment line. Categories grouped (debug/explain/review/plan).
# Information equivalence: each prompt has one expected technical answer regardless of register.
#
# TRAIN/TEST SEPARATION: this is the *report* corpus. It is held out from rule
# tuning — never tune rules/hi/*.md against these prompts. Use prompts/hi.txt
# (the dev corpus) for tuning. Headline numbers should come from this report set.

# --- debug ---
React में input में typing करते ही पूरा form re-render होने का कारण और समाधान समझाओ।
Node.js server start होते समय `EADDRINUSE` error आने का कारण और समाधान की प्रक्रिया बताओ।
TypeScript में `Type 'string | undefined' is not assignable to type 'string'` error को सुरक्षित तरीक़े से हल करने का उपाय क्या है?

# --- explain ---
JavaScript के event delegation का कार्य-सिद्धांत और लाभ समझाओ।
transaction isolation level `READ COMMITTED` और `REPEATABLE READ` का अंतर बताओ।
React की reconciliation में `key` क्यों महत्वपूर्ण है समझाओ।

# --- review ---
निम्न code की समस्या बताओ: ```js\nconst cache = {}; function memo(fn) { return (n) => { if (cache[n]) return cache[n]; return cache[n] = fn(n); }; }\n```
`arr.forEach(async (x) => { await save(x); })` से क्रमिक save की अपेक्षा वाले code की समस्या क्या है?

# --- plan ---
single PostgreSQL को read replica से scale करते समय पहले देखने योग्य 3 जाँच-बिंदु बताओ।
REST API में rate limiting लागू करते समय algorithm चुनने का मापदंड बताओ।
CI pipeline का build time बढ़ जाने पर उसे घटाने की प्राथमिकता बताओ।
