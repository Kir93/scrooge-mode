# Scrooge doc-generation benchmark corpus — EN (held-out; document / prose artifacts).
#
# Each non-comment line is ONE self-contained document-GENERATION task — the surface
# the doc-compression `## Boundaries` rule targets, distinct from prompts/en.txt
# (conversational Q&A). Information is PINNED in every prompt so `normal` and
# `scrooge:en/full` must convey the same facts; the only variable is register /
# verbosity. Disjoint from the tuning corpus — never tune rules against this set.
# `\n` expands to a newline (run.py load_prompts). Mirror of ko-docgen.txt.

Write the "Installation" section of a README. Package `taskline` (npm). Requirements: Node 18+, install `npm i taskline`, initialize `npx taskline init` (creates `taskline.config.json`), env var `TASKLINE_TOKEN` required (startup fails without it). Include an install command code block and a minimal usage example.
Write a feature specification document. Feature: dark mode toggle. Four requirements — (1) toggle button at the header top-right, (2) the choice persists in `localStorage` key `theme`, (3) first visit follows the OS `prefers-color-scheme`, (4) switching uses a 200ms transition. Non-goal: per-theme custom color editor. Include acceptance criteria.
Write the API reference for this REST endpoint. `POST /api/v1/orders`. Body: `items` (array, required), `coupon` (string, optional), `idempotency_key` (string, required). Responses: 201 created order, 400 validation error, 409 duplicate key, 402 payment failed. Auth: `Authorization: Bearer <token>`. Include request and response examples.
Write a migration guide. `charts` library v2 → v3. Four breaking changes — (1) `<Chart data>` prop renamed to `series`, (2) `colors` array → `theme` object, (3) `onClick(idx)` → `onClick(event, point)`, (4) CommonJS build removed (ESM only). Include before/after code per change and the migration order.
Write an architecture overview document. System: a URL shortener service. Five components — API gateway, shortener service (base62 encoding), Redis cache, PostgreSQL (stores original URLs), analytics worker (aggregates clicks asynchronously). Include the request flow (create and redirect) and each component's role.
Write release notes. `taskline` v2.3.0. Six changes — feat: recurring tasks, feat: CSV export, fix: timezone bug on past-midnight deadlines, fix: crash on empty-title tasks, perf: 2x faster list render, chore: drop Node 16 support. Group entries by user impact.
Write a troubleshooting guide. CLI tool `taskline`. Three common errors — (1) `TASKLINE_TOKEN not set` (cause: missing env var, fix: export then re-run), (2) `config not found` (cause: init not run, fix: `npx taskline init`), (3) `409 sync conflict` (cause: concurrent edits on another device, fix: `taskline pull --rebase`). Include symptom, cause, and fix per error.
Write a new-developer onboarding document. Five local-setup steps — (1) clone the repo, (2) `pnpm install`, (3) copy `.env.example` to `.env` and set `DATABASE_URL`, (4) `pnpm db:migrate`, (5) `pnpm dev` (localhost:3000). Include the command and a verification check per step.
Write a configuration reference. `taskline.config.json` with six options — `token` (string, required), `syncInterval` (number, default 300, seconds), `theme` (`light`|`dark`, default `light`), `notify` (boolean, default true), `maxItems` (number, default 1000), `endpoint` (string, default the official API). Include a table of each option's type, default, and purpose.
Write an ADR (architecture decision record). Decision: adopt Redis for session storage (alternatives: in-memory, DB sessions). Context: need horizontal scaling across multiple instances. Rationale: shared state, TTL expiry, low latency. Consequences: adds a Redis operational dependency, sessions lost on outage (accepted). Status accepted.
Write component usage documentation. `<Button>` (React). Four props — `variant` (`primary`|`secondary`|`ghost`, default `primary`), `size` (`sm`|`md`|`lg`, default `md`), `disabled` (boolean), `onClick` (function). Include three usage examples (default, disabled, ghost+sm).
Write a deployment runbook. Five production-deploy steps — (1) `npm run build` on `main`, (2) staging smoke test, (3) `npm run deploy:prod`, (4) health check `/healthz` returns 200, (5) monitor error rate for 5 minutes. Rollback: `npm run deploy:rollback` (reverts to the previous release). Include the command and a failure response per step.
