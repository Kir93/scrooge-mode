# Scrooge benchmark — JA report corpus (HELD-OUT)
# One prompt per non-comment line. Categories grouped (debug/explain/review/plan).
# Information equivalence: each prompt has one expected technical answer regardless of register.
#
# TRAIN/TEST SEPARATION: this is the *report* corpus. It is held out from rule
# tuning — never tune rules/ja/*.md against these prompts. Use prompts/ja.txt
# (the dev corpus) for tuning. Headline numbers should come from this report set.

# --- debug ---
React で input にタイピングするたびにフォーム全体が再レンダリングされる原因と解決策を説明して。
Node.js サーバ起動時に `EADDRINUSE` エラーが出る原因と解決手順を教えて。
TypeScript で `Type 'string | undefined' is not assignable to type 'string'` エラーを安全に解消する方法は？

# --- explain ---
JavaScript の event delegation の動作原理と利点を説明して。
トランザクション分離レベル `READ COMMITTED` と `REPEATABLE READ` の違いを教えて。
React の reconciliation で `key` がなぜ重要か説明して。

# --- review ---
次のコードの問題点を教えて: ```js\nconst cache = {}; function memo(fn) { return (n) => { if (cache[n]) return cache[n]; return cache[n] = fn(n); }; }\n```
`arr.forEach(async (x) => { await save(x); })` で順次保存を期待するコードの問題点は？

# --- plan ---
単一 PostgreSQL を read replica で拡張する時、最初に見るべき点検項目を3つ教えて。
REST API に rate limiting を導入する時のアルゴリズム選択基準を教えて。
CI パイプラインのビルド時間が長くなった時の短縮優先順位を教えて。
